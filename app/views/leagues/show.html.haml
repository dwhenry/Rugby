.header
  League:
  = @league.name

.display_row.row_header
  .position
    Position
  .points
    Points
  .name
    Name
- pos, points, next_pos = 0, -1, 0
- @league.users_by_position.each_with_index do |user, i|
  - next_pos += 1
  - pos = next_pos unless user.points == points
  - points = user.points
  .display_row
    .position
      = pos
    .points
      = user.points
    .name
      = user.name || user.login

